<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Library</OutputType>
    <TargetFrameworks>net8.0;net9.0;net10.0</TargetFrameworks>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
  </PropertyGroup>

  <!-- NuGet Package Metadata -->
  <PropertyGroup>
    <PackageId>AgentNet.InProcess</PackageId>
    <Version>$(AgentNetInProcessVersion)</Version>
    <Authors>Jordan Marr</Authors>
    <Description>In-process workflow execution for AgentNet. Compiles F# workflow definitions to Microsoft Agent Framework (MAF) format and executes them in-process.</Description>
    <PackageTags>fsharp;ai;agents;llm;microsoft-agent-framework;workflows;in-process</PackageTags>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageProjectUrl>https://github.com/JordanMarr/Agent.NET</PackageProjectUrl>
    <RepositoryUrl>https://github.com/JordanMarr/Agent.NET</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="WorkflowBuilderExtensions.fs" />
    <Compile Include="Workflow.InProcess.fs" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="AgentNet.Tests" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\AgentNet\AgentNet.fsproj" />
    <ProjectReference Include="..\AgentNet.Workflow.Abstractions\AgentNet.Workflow.Abstractions.csproj" PrivateAssets="All" />
    <ProjectReference Include="..\AgentNet.InProcess.Interop\AgentNet.InProcess.Interop.csproj" PrivateAssets="All" />
    <!-- Compile-time only: needed because PackedTypedStep references IExecutor in its type signature.
         NOT bundled in the nupkg — only loaded at runtime when AgentNet.Durable is used. -->
    <ProjectReference Include="..\AgentNet.Durable.Interop\AgentNet.Durable.Interop.csproj" PrivateAssets="All" />
  </ItemGroup>

  <!-- Include interop DLLs in NuGet package -->
  <Target Name="IncludeInteropInPackage" BeforeTargets="BuildOnlySettings">
    <ItemGroup>
      <BuildOutputInPackage Include="$(OutputPath)AgentNet.Workflow.Abstractions.dll" />
      <BuildOutputInPackage Include="$(OutputPath)AgentNet.InProcess.Interop.dll" />
    </ItemGroup>
  </Target>

  <ItemGroup>
    <PackageReference Include="Microsoft.Agents.AI.Workflows" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Update="FSharp.Core" Version="8.0.100" />
  </ItemGroup>

</Project>
